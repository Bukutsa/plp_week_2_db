# PLP WEEK TWO DATABASE ASSIGNMENT

```
answers.sql - # sql query answers to questions
    Q1 - retrieve the checkNumber, paymentDate, and amount from the payments table.
    
    Q2 - retrieve the orderDate, requiredDate, and status of orders that are currently 'In Process' from the orders table. Sort the results in descending order of orderDate

    Q3 - display the firstName, lastName, and email of employees whose job title is 'Sales Rep' and order them in descending order of employeeNumber

    Q4 - retrieve all the columns and records from the offices table

    Q5 - fetch the productName and quantityInStock from the products table. Sort the results in ascending order of buyPrice and limit the output to 5 records
    ```